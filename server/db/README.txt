There should be 3 .db files in this directory.
Each of these files is a SQLite database.
    default.db is a starter DB with 2 base decks preloaded
    empty.db is an empty SQLite database
    game.db is a copy of default.db that we can edit